language: generic
sudo: required 

services:
  - docker 

before_install:
  - docker build -t andrey1961/docker-react -f Dockerfile.dev .  

script:
  - docker run -e CI=true andrey1961/docker-react npm run test